// tmux-web — frontend logic — DEPRECATED.
//
// Контент перенесён в /js/ (Phase 1 ES Modules refactor).
// Файл оставлен для cache-warmth: старые открытые вкладки браузера могут
// продолжать дергать /app.js до hard-reload — пусть отдают пустой скрипт,
// а не 404.
//
// Маппинг функций → новые модули см. в `arhit doc show refactor-ui-modules`.
// Главная точка входа — `/js/main.js` (тип module, подключается в index.html).
