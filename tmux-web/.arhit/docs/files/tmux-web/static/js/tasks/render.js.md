# tmux-web/static/js/tasks/render.js

Phase 1. Tasks kanban render: TASK_COLUMNS=[todo,open,in_progress,blocked,deferred,draft,closed], COLUMN_TITLES. renderTasks (board), compareIssues (priority asc, updated_at desc), renderColumn, renderTodoCard, renderCard. currentTodosProjectId возвращает state.activeProjectId. ИЗМЕНЕНИЕ (forge-ja22): убран CLOSED_LIMIT — closed колонка показывает все задачи. ИЗМЕНЕНИЕ (forge-caz3): renderColumn рендерит .col-clean кнопку в header каждой колонки если items.length>0. Поведение: confirm-диалог с количеством задач, далее cleanColumn(status, ids). Для status=closed → bulk purge через POST /api/tasks/:id/purge. Для status=todo → bulk DELETE /api/todos/:id. Для остальных → bulk closeTask с reason='clean-column'. Во время операции btn.disabled=true. При частичном fail — alert со счётчиками ok/fail.
