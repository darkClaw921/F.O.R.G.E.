# tmux-web/src/todos.rs

TODO-карточки с группировкой по root_path (Phase 1 — переход с project_id на cwd-derived path). Хранение перенесено с <project_root>/.forge/todos.json на глобальный ~/.config/forge/todos.json (как projects.json/server_config.json). Структура Todo теперь содержит root_path вместо project_id; #[serde(alias='project_id')] обеспечивает backward-compat для старых файлов. TodoStore.by_path: HashMap<String, Vec<Todo>>. Методы: new(initial_root) / load(file_path) / load_with_projects(file_path, projects_path) / list(root_path) / list_by_cwd(cwd) / get(id) / create(root_path, ...) / create_by_cwd(cwd, ...) / update(id, ...) / delete(id) / save(). Миграция в load_with_projects: если в JSON встречается project_id или хотя бы один root_path не выглядит абсолютным путём, читаем projects.json через load_projects_path_map, переносим Todo в новые buckets и пишем файл в новом формате. Fallback: при отсутствии projects.json project_id остаётся как root_path (deg-fallback, данные не теряются, tracing::warn).
