# tmux-web/static/js/sidebar/sidebar.js

Phase 1. Render sidebar: renderSidebar (legacy folder-grouping), renderSidebarWithOrigin (origin-mode), renderOriginSection. Учитывает state.projectFilter, isRemoteMode(). Использует buildSessionItem + groupSessionsByFolder.
