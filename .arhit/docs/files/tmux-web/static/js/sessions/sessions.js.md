# tmux-web/static/js/sessions/sessions.js

Phase 1. Sessions REST/render: fetchSessions, buildSessionItem, groupSessionsByFolder, startPolling(3s), stopPolling, createSessionPrompt, renameSession, killSession, openSession (с auto switchActiveProject если local), switchSession. Origin-aware через apiFetch+dtoOrigin.
