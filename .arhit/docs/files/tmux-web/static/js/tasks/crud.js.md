# tmux-web/static/js/tasks/crud.js

Phase 1. Tasks CRUD + optimistic UI: getIssueIndex, applyOptimisticPatch (mutates+render), rollbackIssue, createTask (POST + temp-id placeholder), updateTask (PATCH с origin через taskOriginById), closeTask (DELETE с reason), reopenTask (POST), promoteTodo (POST /api/todos/:id/promote с auto-resolve session).
