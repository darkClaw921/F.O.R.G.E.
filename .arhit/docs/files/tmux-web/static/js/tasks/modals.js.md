# tmux-web/static/js/tasks/modals.js

Phase 1. Tasks modals: buildTaskFormHtml (общий шаблон HTML — title/desc/prio/type/labels/[status]), openCreateModal (с preset.status; TODO режим=POST /api/todos), openEditModal (PATCH через updateTask, Close/Reopen кнопки), openTodoEditModal (Save/Delete/Promote, plan_mode checkbox).
