# tmux-web/static/js/terminal/theme-mapper.js

Phase 1. Pure helper — mapTermTheme(t) маппит наш TermColors (camelCase) в xterm.js ITheme. Единственная переименовка: selection → selectionBackground. 1:1 копия из app.js:362.
