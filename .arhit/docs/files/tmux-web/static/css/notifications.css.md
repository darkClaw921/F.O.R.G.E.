# tmux-web/static/css/notifications.css

Notifications form в Settings modal (Phase 5): .btn-settings (мелкие кнопки внутри settings), .notify-* классы (формы per-project настроек уведомлений: enable/disable, ringtone select, hooks textarea), .notify-actions (кнопки save/cancel/test, :disabled = opacity 0.5). 124 строки. Используется на 'Notifications' вкладке settings-модала.
