# tmux-web/static/css/settings-modal.css

Settings modal (Phase wk7.4 + Phase 5): tab-bar внутри settings-модала (Notifications / Themes / Remote servers — более тонкие чем глобальный #tab-bar), Themes-вкладка с пресетами и .theme-card/.theme-card-tool, и Remote servers tab — .remotes-table, .remote-server-form, .remote-row :hover/.active. 457 строк (объединены 1518-1769 — settings tab-bar и themes panel — и 2709-2913 — Remote servers tab; они логически принадлежат одному модалу).
