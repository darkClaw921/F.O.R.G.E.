Фикс отображения главного экрана #home при отсутствии активных сессий (2026-05-27). Два дефекта в CSS: (1) #main имел position:static, из-за чего .home и .placeholder (position:absolute; inset:0) anchor'ились к вьюпорту и перекрывали сайдбар + таб-бар; добавлен position:relative в layout.css#main → home теперь ограничен областью #main (x=240). (2) .home-empty имел display:flex, который перекрывал UA-шный display:none для атрибута [hidden], поэтому 'История пуста' висела одновременно с карточками; добавлено правило .home-empty[hidden]{display:none} в home.css. renderHome переключает видимость пустого состояния через $homeEmpty.hidden. Изменены файлы: tmux-web/static/css/layout.css, tmux-web/static/css/home.css. Проверено через playwright (mock /api/sessions=[]): home показывается в #main, сайдбар виден, пустое состояние корректно скрывается/показывается.