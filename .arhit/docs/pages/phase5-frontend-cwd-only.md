Phase 5 frontend-cleanup: переход на cwd-only UI без projects-концепта. Удалён модуль tmux-web/static/js/projects/ (projects.js, new-project.js). Удалены ссылки из bootstrap.js (fetchProjects, openNewProjectModal, projectSelect change handler), sessions.js (switchActiveProject branch в openSession), settings/modal.js (project-list), tabs/tui-tabs.js (getActiveProject и project.path fallback). state.js: убраны поля activeProjectId, projects, projectFilter, remoteProjects; todosCurrentProjectId переименован в todosCurrentPath. index.html: project-bar (#project-select, #project-new) удалён, оставлен #project-settings для глобального settings modal; placeholder-тексты Git/Docker/Telescope обновлены 'Select a session'. Sidebar.js полностью переписан — только folder-группировка через renderFolderGroups, orphan-сессии плоско или под 'All sessions' заголовком. ws/todos-ws.js: ?path=<abs> вместо ?project_id=, sessionPathOrNull, syncTodosToCurrentSession сравнивает по path. ws/tasks-ws.js: упрощён без activeProjectId fallback. tasks/modals.js и tasks/crud.js: TODO создаётся с body.path = sess.path активной сессии, session-fallback ищет по любым сессиям без фильтра по project. settings/notifications-tab.js переписан: глобальный NotifierConfig (GET/PATCH /api/notifier-config) вместо per-project notify_*. remote/servers.js: loadRemoteProjects удалён, aggregateAllOrigins без projects. sidebar/origin-tabs.js: убран loadRemoteProjects при выборе origin.