# main.rs::get_todos

Phase 3 — GET /api/todos?project_id=...: JSON-массив TODO-карточек проекта. Если project_id не задан — берётся active project. Возвращает пустой массив (не 404) если в проекте нет TODO.
