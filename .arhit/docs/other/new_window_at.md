# new_window_at

tmux.rs: создаёт окно по явному индексу (tmux new-window -t session:index). Сохраняет оригинальные индексы окон при восстановлении сессии из истории. Caller обязан гарантировать свободный индекс.
