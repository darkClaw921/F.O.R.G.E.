# server_config

server_config — резолюция EffectiveConfig.pwa_enabled (CLI > file > env DEVFORGE_PWA > default false; логическое ИЛИ, не first-wins). Edge-тесты (42): CLI true перебивает file:false; pwa ортогонален remote_mode; env truthy побеждает file:false (нет veto); мусорный env не включает; env_flag_is_truthy расширенная матрица (регистр/пробелы/мусор); pwa_env_matrix — ЕДИНСТВЕННЫЙ env-мутирующий тест (вся DEVFORGE_PWA-матрица в одном последовательном #[test] с восстановлением env, т.к. env не thread-safe при параллельном cargo test); print_public_bind_warning ветки pwa true/false × localhost/public × битый/короткий/пустой токен — no-panic, web-push строка только при public+pwa_enabled, localhost → ранний return.
